let n 10 end # size
let i 0 end     
let v1 0 end  
let v2 1 end
let t 0 end

# performs this loop until !(i < n)
while i n < do
    # set t = v1
    &t v1 =

    # print the current value in the sequence
    t ^

    # v1 = v1 + v2
    &v1 v1 v2 + =

    # v2 = t
    &v2 t =

    # i = i + 1
    &i i ++ =
end
