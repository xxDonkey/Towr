let n 100 end
let isquared 1 end
let i 1 end

# performs this loop until !(isquared < n)
while isquared n < do
    # print the current square
    isquared ^

    # i = i + 1
    &i i ++ =

    # isquared = i * i
    &isquared i i * =
end
